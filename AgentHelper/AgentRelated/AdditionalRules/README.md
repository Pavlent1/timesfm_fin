# Additional Rules

This folder contains topic-scoped rules for `AgentHelper`.

Load only the rule folders relevant to the current task. Do not load every rule folder by default.

Current topics:

- `Governance/`
- `Git/`
- `Skills/`
- `Testing/`

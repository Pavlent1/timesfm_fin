# Quick Task 260416-1nx: add per-step direction-above-below-last-input accuracy metric to backtests

**Created:** 2026-04-15
**Status:** Completed

## Tasks

### 1. Extend shared backtest step metrics
- files: `src/backtest_metrics.py`, `tests/test_backtest_metrics.py`
- action: add reusable logic that checks whether predicted and actual closes finish on the same side of the last input close and expose it in `build_step_metrics()`
- verify: unit tests cover above, below, and match cases
- done: runtime and SQL aggregation can reuse one direction-guess contract

### 2. Surface the metric in both report paths
- files: `src/crypto_prediction_backtest.py`, `src/crypto_minute_backtest.py`, `src/postgres_backtest.py`, `db/init/002_phase2_backtest_schema.sql`
- action: include the new per-step flag in in-memory detail rows and expose a per-output-candle `direction_guess_accuracy_pct` aggregate in direct and PostgreSQL-backed reports
- verify: report tests and PostgreSQL stats tests see the new column
- done: per-step stats tables show percent-correct direction guesses for each output candle

### 3. Refresh tests and descriptions
- files: `tests/test_crypto_prediction_backtest.py`, `tests/test_crypto_minute_backtest.py`, `tests/test_postgres_backtest.py`, `AgentHelper/ProjectFiles/DescriptionFiles/src/*.md`
- action: update affected tests and approved-scope descriptions for the new metric
- verify: targeted pytest slice passes
- done: changed files and descriptions agree on the new behavior

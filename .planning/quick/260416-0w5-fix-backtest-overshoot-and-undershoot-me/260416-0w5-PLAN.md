# Quick Task 260416-0w5: fix backtest overshoot and undershoot metrics to report average percent magnitude

**Created:** 2026-04-15
**Status:** Completed

## Tasks

### 1. Extend shared metric semantics
- files: `src/backtest_metrics.py`
- action: keep overshoot classification as-is and expose reusable percent-magnitude helpers for overshoot and undershoot summaries
- verify: unit tests cover the new helper behavior and `build_step_metrics()` contract
- done: backtest code can derive average overshoot and average undershoot percentages without reinterpreting labels

### 2. Update both backtest summary paths
- files: `src/crypto_prediction_backtest.py`, `src/crypto_minute_backtest.py`, `src/postgres_backtest.py`, `db/init/002_phase2_backtest_schema.sql`
- action: replace count-only overshoot and undershoot summary reporting with average percentage magnitudes in both direct-Python and PostgreSQL-backed paths
- verify: targeted tests assert the new summary fields and rendered reports include them
- done: FM and strategy backtests surface average overshoot and undershoot percentages in saved reports

### 3. Refresh tests and descriptions
- files: `tests/test_backtest_metrics.py`, `tests/test_crypto_prediction_backtest.py`, `tests/test_crypto_minute_backtest.py`, `tests/test_postgres_backtest.py`, `AgentHelper/ProjectFiles/DescriptionFiles/src/*.md`
- action: update the affected tests and touched file descriptions to match the new reporting contract
- verify: targeted pytest slice passes
- done: changed approved-scope files and their descriptions agree on the new metric behavior

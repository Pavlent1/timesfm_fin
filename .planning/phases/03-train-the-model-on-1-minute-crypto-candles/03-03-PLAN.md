---
phase: 03-train-the-model-on-1-minute-crypto-candles
plan: "03"
type: execute
wave: 3
depends_on:
  - "03-02"
files_modified:
  - requirements.training.txt
  - src/training_environment.py
  - AgentHelper/ProjectFiles/DescriptionFiles/src/training_environment.py.md
  - tests/test_training_workflow.py
autonomous: true
requirements:
  - MODEL-01
  - OPS-01
user_setup:
  - The operator can inspect or build the intended Python 3.10 runtime for the legacy TimesFM v1 training stack
must_haves:
  truths:
    - The supported manual training environment is frozen in a checked-in requirements file instead of floating to whatever JAX, PAX, or TensorFlow versions happen to install that day.
    - Phase 3 has one reusable helper for recording Python version, package snapshot, git commit, config path, and prepared-bundle identity for each manual run.
    - The environment snapshot format is stable enough for later wrapper and lineage plans to consume without redefining reproducibility metadata.
    - The workflow remains manual-only and does not introduce scheduled retraining or service orchestration.
  artifacts:
    - requirements.training.txt defines the exact supported Phase 3 manual training environment input for Python 3.10 and the legacy TimesFM v1 stack.
    - src/training_environment.py captures reproducibility details for the active run.
    - tests/test_training_workflow.py locks the environment-freeze and run-environment capture contract before wrapper code is added.
  key_links:
    - src/training_environment.py records the prepared-bundle manifest identity emitted by 03-02 so later training and comparison steps can bind environment facts to a concrete dataset slice.
    - requirements.training.txt freezes the intended Python 3.10 plus `timesfm[pax]==1.3.0` training input and companion JAX/PAX/TensorFlow tuple instead of leaving "latest compatible" packages implicit.
    - Later plans 03-04 and 03-05 consume the same environment snapshot shape instead of inventing a second metadata schema.
---

<objective>
Freeze the supported manual training environment and define how Phase 3 records reproducibility facts before any wrapper launches the legacy trainer.

Purpose: Phase 3 needs an explicit, reusable environment contract before the manual wrapper exists, because later training and comparison steps cannot make credible reproducibility claims if package resolution and runtime capture remain implicit.
Output: A frozen training requirements file, an environment-capture helper, and tests that lock the environment snapshot contract for later plans.
</objective>

<execution_context>
@C:/Users/Pavel/Documents/GitHub/timesfm_fin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Pavel/Documents/GitHub/timesfm_fin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-train-the-model-on-1-minute-crypto-candles/03-CONTEXT.md
@.planning/phases/03-train-the-model-on-1-minute-crypto-candles/03-RESEARCH.md
@.planning/phases/03-train-the-model-on-1-minute-crypto-candles/03-VALIDATION.md
@.planning/phases/03-train-the-model-on-1-minute-crypto-candles/03-02-PLAN.md
@configs/fine_tuning.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the training-environment contract tests</name>
  <files>tests/test_training_workflow.py</files>
  <action>Create pytest coverage that defines the Phase 3 training-environment contract before implementation. Lock the checked-in package freeze requirement, the environment helper's Python/package/git/config capture behavior, prepared-bundle manifest recording, and manual-only behavior per D-19/D-20. Keep the tests collection-safe before implementation by using lazy imports or another no-import-at-collection pattern for modules that do not exist until later tasks.</action>
  <verify>
    <automated>.\.venv\Scripts\python.exe -m pytest --collect-only -q tests/test_training_workflow.py</automated>
  </verify>
  <done>The Phase 3 environment-freeze contract is defined in collection-safe tests before runtime-capture code is added.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the training environment spec and capture helper</name>
  <files>requirements.training.txt, src/training_environment.py</files>
  <behavior>
    - Test 1: the environment helper records Python version, package snapshot, and key runtime metadata for each run.
    - Test 2: the run bundle can point back to the requirements file used to create the environment.
    - Test 3: the requirements file freezes the known-good Python 3.10 + TimesFM v1 / JAX / PAX / TensorFlow install input instead of floating to latest packages.
  </behavior>
  <action>Create `requirements.training.txt` for the supported manual Phase 3 training stack and add `src/training_environment.py` to capture per-run reproducibility details such as Python version, `pip freeze`, git commit, config path, and bundle-manifest identity. Use this task to resolve the remaining environment question from `03-RESEARCH.md`: the file must freeze the intended Python 3.10 + `timesfm[pax]==1.3.0` training input and the accompanying JAX/PAX/TensorFlow tuple used by the later wrapper, rather than leaving "latest compatible" packages implicit. This satisfies the reproducibility-only portion of OPS-01 without planning scheduled retraining, and it gives later training and comparison code a stable environment snapshot to diff across runs. Create the mirrored AgentHelper description for `src/training_environment.py` after its run-capture responsibility is implemented.</action>
  <verify>
    <automated>.\.venv\Scripts\python.exe -m pytest -q tests/test_training_workflow.py -k environment -x</automated>
  </verify>
  <done>The repo has an explicit manual training environment input plus a helper that records what environment a run actually used.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local environment -> run metadata | Captured environment facts are the basis for later reproducibility and comparison claims |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | R | reproducibility claims | mitigate | Capture Python/package/runtime metadata in a machine-readable file for each run and bind it to the bundle identity and command used. |
</threat_model>

<verification>
Run `.\.venv\Scripts\python.exe -m pytest --collect-only -q tests/test_training_workflow.py`.
Run `.\.venv\Scripts\python.exe -m pytest -q tests/test_training_workflow.py -k environment -x`.
Run `node scripts/precommit-checks.mjs`.
</verification>

<success_criteria>
The repo has a checked-in, explicit Phase 3 training environment spec, each run can capture Python/package/git/config/bundle identity without adding scheduled retraining, and later wrapper/comparison plans can reuse that environment snapshot instead of inferring it from the host.
</success_criteria>

<output>
After completion, create `.planning/phases/03-train-the-model-on-1-minute-crypto-candles/03-03-SUMMARY.md`
</output>

---
phase: 03-train-the-model-on-1-minute-crypto-candles
plan: "03"
type: execute
wave: 3
depends_on:
  - "03-02"
files_modified:
  - requirements.training.txt
  - src/training_environment.py
  - src/train_from_postgres.py
  - src/evaluate_training_run.py
  - src/backtest_training_run.py
  - src/main.py
  - AgentHelper/ProjectFiles/DescriptionFiles/src/training_environment.py.md
  - AgentHelper/ProjectFiles/DescriptionFiles/src/train_from_postgres.py.md
  - AgentHelper/ProjectFiles/DescriptionFiles/src/evaluate_training_run.py.md
  - AgentHelper/ProjectFiles/DescriptionFiles/src/backtest_training_run.py.md
  - AgentHelper/ProjectFiles/DescriptionFiles/src/main.py.md
  - tests/test_training_workflow.py
autonomous: true
requirements:
  - MODEL-01
  - OPS-01
user_setup:
  - A Python 3.10 training environment is created manually and can install `timesfm[pax]==1.3.0` plus the repo training dependencies
  - The chosen runtime is Linux, WSL, or Docker-compatible with the legacy TimesFM v1 training stack
must_haves:
  truths:
    - The operator can launch a manual fine-tune run from a prepared PostgreSQL-derived bundle without inventing a new training engine.
    - Every run captures an explicit parent checkpoint choice, copied config, bundle identity, and resolved environment details in a deterministic run directory.
    - The manual training environment is frozen through a checked-in requirements file instead of floating to whatever JAX/PAX/TensorFlow versions happen to install that day.
    - The wrapper validates bundle sample count before launch and derives or writes a compatible batch size so the D-07 one-month-per-symbol starter run cannot silently collapse to `train_size=0`.
    - Each completed run records canonical holdout evaluation and checkpoint-linked backtest outputs, not just trainer metadata, before lineage reporting begins.
    - The workflow remains manual-only and does not introduce scheduled retraining or service orchestration.
  artifacts:
    - requirements.training.txt defines the exact supported Phase 3 manual training environment input for Python 3.10 and the legacy TimesFM v1 stack.
    - src/training_environment.py captures reproducibility details for the active run.
    - src/train_from_postgres.py wraps the existing training entrypoint for prepared bundle execution and orchestrates post-train summary generation.
    - src/evaluate_training_run.py evaluates the trained checkpoint against the explicit holdout artifact from 03-02 and writes `evaluation_summary.json`.
    - src/backtest_training_run.py backtests the trained checkpoint against the explicit holdout/backtest slice and writes `backtest_summary.json`, with `backtest_run_id` recorded as supplemental provenance.
    - tests/test_training_workflow.py locks the wrapper, environment, parent-checkpoint, batch-size, and post-train summary contract.
  key_links:
    - src/train_from_postgres.py invokes src/main.py rather than replacing src/train.py with a new engine.
    - src/train_from_postgres.py rejects missing parent checkpoint input so the wrapper never inherits src/main.py's hidden default.
    - src/train_from_postgres.py consumes sample-count metadata emitted by 03-02 and writes a compatible copied config or fails fast before training starts.
    - src/main.py accepts an explicit parent checkpoint identity that the wrapper records in the run metadata.
    - src/evaluate_training_run.py consumes the explicit holdout artifact from 03-02 instead of relying on src/evaluation.py's random-split path.
    - src/backtest_training_run.py adapts the trained local checkpoint into a Phase 2-compatible backtest flow instead of assuming the existing backtest CLI already accepts local fine-tuned checkpoints.
    - The run bundle records that trainer shuffle eval is not the explicit Phase 3 holdout metric and stores real `evaluation_summary.json` and `backtest_summary.json` outputs for 03-04 to summarize.
---

<objective>
Resolve the remaining parent-checkpoint and environment-freeze questions while wrapping the legacy TimesFM v1 trainer in a reproducible manual workflow that also handles starter-run viability and canonical post-train evaluation.

Purpose: Phase 3 needs a repeatable operator path for running manual retrains from PostgreSQL-derived bundles while preserving environment and lineage facts that the current research entrypoint does not record, without leaving the wrapper parent or dependency stack implicit, and without relying on non-canonical trainer shuffle eval as the only run outcome.
Output: A manual training wrapper, an environment-capture helper, a training requirements file, real holdout evaluation/backtest outputs, and tests that lock the run-bundle contract.
</objective>

<execution_context>
@C:/Users/Pavel/Documents/GitHub/timesfm_fin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Pavel/Documents/GitHub/timesfm_fin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-train-the-model-on-1-minute-crypto-candles/03-CONTEXT.md
@.planning/phases/03-train-the-model-on-1-minute-crypto-candles/03-RESEARCH.md
@.planning/phases/03-train-the-model-on-1-minute-crypto-candles/03-VALIDATION.md
@.planning/phases/03-train-the-model-on-1-minute-crypto-candles/03-02-PLAN.md
@src/main.py
@src/train.py
@src/evaluation.py
@src/crypto_minute_backtest.py
@src/run_forecast.py
@src/postgres_backtest.py
@configs/fine_tuning.py

<interfaces>
From src/main.py:
```python
flags.DEFINE_string('workdir', None, 'Directory to store model data.')
flags.DEFINE_string('dataset_path', None, 'Path to training/test dataset')
flags.DEFINE_bool('do_eval', False, 'Evaluation mode.')
flags.DEFINE_string('checkpoint_path', None, 'Path to checkpoint.')
```

From src/train.py:
```python
def train_and_evaluate(model: Any, config: py_utils.NestedMap, workdir: str, num_classes=2, plus_one=False) -> None:
    train_loader, eval_loader, train_size = preprocess_csv(config.dataset_path, batch_size=config.batch_size)
```

From src/evaluation.py:
```python
def restore_and_evaluate(
    model: Any,
    config: py_utils.NestedMap,
    workdir: str,
    num_classes=2,
    plus_one=True,
    horizons=[128, 64, 32, 16, 8, 4, 2],
) -> None
```

From src/crypto_minute_backtest.py:
```python
def run_backtest(args: argparse.Namespace) -> dict[str, object]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the manual training workflow tests</name>
  <files>tests/test_training_workflow.py</files>
  <action>Create pytest coverage that defines the Phase 3 manual training wrapper contract before implementation. Lock explicit parent checkpoint capture per D-16/D-17, deterministic run-directory layout, copied config and environment manifests, and manual-only behavior per D-19/D-20. Include assertions that the wrapper rejects missing parent checkpoint input instead of inheriting `src/main.py`'s implicit Google default, validates or derives a compatible batch size from the prepared bundle's sample counts before launch, records the prepared bundle manifest path, and labels the trainer's internal shuffle eval as non-canonical holdout output because the existing random split cannot serve as the real holdout strategy. Also lock that a completed run produces real `evaluation_summary.json` and `backtest_summary.json` artifacts tied to the trained checkpoint, with `backtest_run_id` recorded only as supplemental provenance. Keep the tests collection-safe before implementation by using lazy imports or another no-import-at-collection pattern for modules that do not exist until later tasks.</action>
  <verify>
    <automated>.\.venv\Scripts\python.exe -m pytest --collect-only -q tests/test_training_workflow.py</automated>
  </verify>
  <done>The Phase 3 manual training workflow is defined in collection-safe tests before wrapper code is added.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the training environment spec and capture helper</name>
  <files>requirements.training.txt, src/training_environment.py</files>
  <behavior>
    - Test 1: the environment helper records Python version, package snapshot, and key runtime metadata for each run.
    - Test 2: the run bundle can point back to the requirements file used to create the environment.
    - Test 3: the requirements file freezes the known-good Python 3.10 + TimesFM v1 / JAX / PAX / TensorFlow install input instead of floating to latest packages.
  </behavior>
  <action>Create `requirements.training.txt` for the supported manual Phase 3 training stack and add `src/training_environment.py` to capture per-run reproducibility details such as Python version, `pip freeze`, git commit, config path, and bundle-manifest identity. Use this task to resolve the remaining environment question from `03-RESEARCH.md`: the file must freeze the intended Python 3.10 + `timesfm[pax]==1.3.0` training input and the accompanying JAX/PAX/TensorFlow tuple used by the wrapper, rather than leaving "latest compatible" packages implicit. This satisfies the reproducibility-only portion of OPS-01 without planning scheduled retraining, and it gives later comparison/reporting code a stable environment snapshot to diff across runs. Create the mirrored AgentHelper description for `src/training_environment.py` after its run-capture responsibility is implemented.</action>
  <verify>
    <automated>.\.venv\Scripts\python.exe -m pytest -q tests/test_training_workflow.py -k environment -x</automated>
  </verify>
  <done>The repo has an explicit manual training environment input plus a helper that records what environment a run actually used.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement the manual PostgreSQL-bundle training wrapper</name>
  <files>src/train_from_postgres.py, src/main.py, src/training_environment.py, src/evaluate_training_run.py, src/backtest_training_run.py</files>
  <behavior>
    - Test 1: the wrapper requires an explicit parent checkpoint identity and records it in the run bundle.
    - Test 2: the wrapper validates emitted window count and derives or writes a compatible batch size before training starts.
    - Test 3: the wrapper launches the existing training entrypoint with the prepared dataset path instead of replacing the engine.
    - Test 4: a dedicated holdout evaluator consumes the explicit holdout artifact from 03-02 and writes `evaluation_summary.json` for the trained checkpoint.
    - Test 5: a dedicated backtest adapter runs the trained local checkpoint through a Phase 2-compatible backtest flow and writes `backtest_summary.json`, with `backtest_run_id` as supplemental provenance.
  </behavior>
  <action>Create `src/train_from_postgres.py` as the manual Phase 3 operator entrypoint that consumes a prepared bundle directory, requires an explicit parent checkpoint identifier per D-16/D-17/D-18, copies the training config into a deterministic run directory, captures environment metadata, and invokes the existing `src/main.py` flow rather than a new training engine. Use sample-count metadata from 03-02 to validate the D-07 starter preset and either derive a compatible batch size for the copied config or fail fast before training starts, so the legacy default `batch_size=1024` cannot silently produce `train_size=0`. Use this task to resolve the remaining parent-checkpoint question from `03-RESEARCH.md` by making the wrapper require an operator-supplied parent checkpoint path or repo identity and storing that choice in the run bundle; do not give the wrapper its own silent default. After training, call a new `src/evaluate_training_run.py` adapter that consumes the explicit holdout artifact from 03-02 and the produced local checkpoint to write `evaluation_summary.json`, and call a new `src/backtest_training_run.py` adapter that evaluates the same local checkpoint through a Phase 2-compatible backtest path for the explicit holdout/backtest slice and writes `backtest_summary.json`. Record any `backtest_run_id` as supplemental provenance, and record that these outputs, not trainer shuffle eval, are the Phase 3 comparison inputs. Modify `src/main.py` only enough to accept the explicit parent checkpoint path or repo identity passed by the wrapper instead of relying on the hidden Google default. Update the mirrored AgentHelper descriptions for `src/train_from_postgres.py`, `src/evaluate_training_run.py`, `src/backtest_training_run.py`, and `src/main.py` once the responsibilities are final. Keep this workflow manual-only per D-19, and do not introduce scheduled retraining or background services per D-20.</action>
  <verify>
    <automated>.\.venv\Scripts\python.exe -m pytest -q tests/test_training_workflow.py -x</automated>
  </verify>
  <done>A manual Phase 3 training run can be launched from a prepared PostgreSQL-derived bundle, every run captures reproducibility and parentage data in one deterministic location, and each completed run includes real `evaluation_summary.json` and `backtest_summary.json` artifacts generated from explicit holdout-aware adapters.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Prepared bundle -> legacy training entrypoint | The generated dataset and selected parent checkpoint directly influence checkpoint outputs |
| Local environment -> run metadata | Captured environment facts are the basis for later reproducibility and comparison claims |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | S/T | parent checkpoint selection | mitigate | Require the wrapper to pass and record an explicit parent checkpoint path or repo identity instead of relying on an implicit default. |
| T-03-08 | T | run directory contents | mitigate | Use deterministic per-run directories with copied config, manifest, and environment snapshots instead of anonymous checkpoint folders. |
| T-03-09 | R | reproducibility claims | mitigate | Capture Python/package/runtime metadata in a machine-readable file for each run and bind it to the bundle identity and command used. |
</threat_model>

<verification>
Run `.\.venv\Scripts\python.exe -m pytest --collect-only -q tests/test_training_workflow.py`.
Run `.\.venv\Scripts\python.exe -m pytest -q tests/test_training_workflow.py -x`.
Run `node scripts/precommit-checks.mjs`.
</verification>

<success_criteria>
The repo can launch a manual fine-tune from a prepared PostgreSQL bundle through the existing TimesFM v1 training engine, each run preserves explicit parentage, config, environment, and command metadata without adding scheduled retraining, the D-07 starter run cannot fail silently from incompatible batch-size defaults, and each run emits real holdout evaluation/backtest outputs for later comparison.
</success_criteria>

<output>
After completion, create `.planning/phases/03-train-the-model-on-1-minute-crypto-candles/03-03-SUMMARY.md`
</output>

---
phase: 03-train-the-model-on-1-minute-crypto-candles
plan: "02"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/training_manifest.py
  - src/postgres_prepare_training.py
  - AgentHelper/ProjectFiles/DescriptionFiles/src/training_manifest.py.md
  - AgentHelper/ProjectFiles/DescriptionFiles/src/postgres_prepare_training.py.md
  - tests/test_training_manifest.py
  - tests/test_training_preparer.py
autonomous: true
requirements:
  - MODEL-01
  - MODEL-02
user_setup:
  - Phase 3 source coverage has been loaded and verified through `03-01`
must_haves:
  truths:
    - The operator can define per-symbol train and holdout ranges explicitly in one manifest instead of relying on one shared calendar slice.
    - The workflow can emit cleaned fixed-length model windows from PostgreSQL in either strict or repair-capable mode.
    - Train and holdout boundaries are explicit in the emitted artifacts and are not treated as the trainer's random split.
    - The emitted bundle metadata makes the D-07 one-month-per-symbol preset operationally viable by exposing window counts that downstream training code can use to validate or derive a compatible batch size.
  artifacts:
    - src/training_manifest.py validates the manifest contract for symbol ranges, holdouts, presets, and cleaning settings.
    - src/postgres_prepare_training.py materializes model-ready window bundles and sidecar reports from PostgreSQL.
    - tests/test_training_manifest.py and tests/test_training_preparer.py lock the Phase 3 preparation contract.
  key_links:
    - src/postgres_prepare_training.py consumes the segment-aware integrity data established in 03-01 before generating windows.
    - src/postgres_prepare_training.py emits bundle artifacts that the legacy training entrypoint can consume through `--dataset_path`.
    - The manifest contract encodes per-symbol holdouts explicitly so later comparison code can link runs back to concrete PostgreSQL slices.
    - The emitted dataset manifest and quality report expose total and per-symbol sample counts so 03-04 can prevent `train_size=0` failures on the initial D-07 starter run.
---

<objective>
Build the manifest-driven cleaning and preparation layer that turns PostgreSQL minute candles into model-ready training bundles.

Purpose: Phase 3 needs a preparation contract that matches the legacy TimesFM v1 trainer's fixed-length positive-window expectations while preserving explicit per-symbol train and holdout rules.
Output: A manifest validator, a PostgreSQL preparer that emits fixed-length windows and quality sidecars, and tests that lock strict versus repair behavior.
</objective>

<execution_context>
@C:/Users/Pavel/Documents/GitHub\timesfm_fin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Pavel/Documents/GitHub\timesfm_fin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-train-the-model-on-1-minute-crypto-candles/03-CONTEXT.md
@.planning/phases/03-train-the-model-on-1-minute-crypto-candles/03-RESEARCH.md
@.planning/phases/03-train-the-model-on-1-minute-crypto-candles/03-VALIDATION.md
@.planning/phases/03-train-the-model-on-1-minute-crypto-candles/03-01-PLAN.md
@src/postgres_materialize_dataset.py
@src/postgres_verify_data.py
@src/train.py
@configs/fine_tuning.py

<interfaces>
From configs/fine_tuning.py:
```python
config.context_len = config.input_len = 512
config.output_len = 128
config.horizon_len = 128
```

From src/train.py:
```python
def preprocess_csv(file_path, batch_size=32, train_ratio=0.75):
    df = pd.read_csv(file_path, dtype='float64')
    df = df.dropna(axis=1, how='any')
    df = df.transpose()
    df = df.sample(frac=1).reset_index(drop=True)
```

From src/postgres_materialize_dataset.py:
```python
def materialize_training_matrix(frame: pd.DataFrame) -> pd.DataFrame:
    pivot = frame.pivot(index="observation_time_utc", columns="series_label", values="close_price")
    pivot = pivot.dropna(axis=0, how="any")
    return pivot.reset_index(drop=True)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write manifest and preparer contract tests</name>
  <files>tests/test_training_manifest.py, tests/test_training_preparer.py</files>
  <action>Create the Phase 3 tests first. Lock per-symbol custom train and holdout ranges per D-05/D-08, easy presets per D-06 including the initial one-month-per-symbol experiment per D-07, the BTC default holdout reserve per D-09, configurable ETH/SOL holdouts per D-10, explicit slice metadata per D-11, and strict versus repair-capable cleaning/report behavior per D-12/D-13/D-14/D-15. Include assertions that emitted train windows are exactly `512 + 128` points long, remain positive-valued for the current log-transform training path, and publish machine-readable sample counts so downstream training can validate starter-run batch-size viability. Keep the tests collection-safe before implementation by using lazy imports or another no-import-at-collection pattern for modules that do not exist until later tasks.</action>
  <verify>
    <automated>.\.venv\Scripts\python.exe -m pytest --collect-only -q tests/test_training_manifest.py tests/test_training_preparer.py</automated>
  </verify>
  <done>The preparation contract is described in collection-safe tests before production code is written.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the manifest contract for per-symbol ranges and holdouts</name>
  <files>src/training_manifest.py</files>
  <behavior>
    - Test 1: manifests reject unsupported symbols, overlapping ranges, inverted timestamps, and missing holdout definitions.
    - Test 2: preset helpers make the initial one-month BTC/ETH/SOL training round easy without replacing custom per-symbol selectors.
    - Test 3: BTC defaults reserve the latest four months while ETH and SOL remain configurable.
  </behavior>
  <action>Create `src/training_manifest.py` as the canonical Phase 3 contract. It must validate custom per-symbol `train_start_utc` / `train_end_utc` and `holdout_start_utc` / `holdout_end_utc` fields per D-05/D-08, preserve simple preset helpers per D-06 including the first intended one-month round per D-07, set the BTC default holdout reserve to the latest four months per D-09, keep ETH/SOL holdouts configurable per D-10, and serialize the exact slice decisions needed for later reproducibility per D-11. Keep the scope manual-only and do not add any scheduled retraining or service behavior. Create the mirrored AgentHelper description for `src/training_manifest.py` after the contract is finalized.</action>
  <verify>
    <automated>.\.venv\Scripts\python.exe -m pytest -q tests/test_training_manifest.py -x</automated>
  </verify>
  <done>The repo has one manifest format that fully describes the per-symbol Phase 3 selection and holdout rules.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement the PostgreSQL cleaner and fixed-window bundle preparer</name>
  <files>src/postgres_prepare_training.py, src/training_manifest.py</files>
  <behavior>
    - Test 1: strict mode rejects ranges with blocking gaps or unsafe repairs.
    - Test 2: repair mode only fixes allowed short-gap cases and records every repair in the quality report.
    - Test 3: emitted train windows are fixed-length 640-point samples and holdout artifacts remain explicitly separate from trainer shuffle eval.
    - Test 4: emitted bundle metadata exposes total and per-symbol sample counts for batch-size validation in 03-04.
  </behavior>
  <action>Create `src/postgres_prepare_training.py` to read Phase 3 manifests, query canonical PostgreSQL candles, reuse the segment/gap intelligence from 03-01, and emit a prepared bundle directory containing cleaned `train_windows.csv`, explicit holdout artifacts, `dataset_manifest.json`, `quality_report.json`, and `window_index.csv`. This preparer must introduce the dedicated cleaning step required by D-12, support strict and repair-capable modes per D-13, emit the canonical cleaned dataset plus quality report per D-14, and concretely satisfy the current trainer contract discovered in research per D-15. Do not reuse the existing `training_matrix` export as the final contract, and do not treat `src/train.py`'s random split as the real holdout strategy. The emitted metadata must include total and per-symbol window counts so 03-04 can validate or derive a compatible batch size for the D-07 starter preset before training starts. Create or update the mirrored AgentHelper descriptions for `src/postgres_prepare_training.py` and `src/training_manifest.py` once the module responsibilities settle.</action>
  <verify>
    <automated>.\.venv\Scripts\python.exe -m pytest -q tests/test_training_manifest.py tests/test_training_preparer.py -x</automated>
  </verify>
  <done>Prepared Phase 3 bundles come from PostgreSQL through one manifest-driven cleaner/preparer contract that preserves explicit holdout boundaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Manifest JSON -> preparation logic | User-selected ranges, holdouts, and cleaning settings decide what data becomes model input |
| PostgreSQL candles -> emitted bundle CSVs | Database content crosses into training artifacts that will influence checkpoint behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | T | manifest inputs | mitigate | Validate the manifest schema strictly, enforce the Phase 3 symbol allowlist, and reject overlapping or inverted date ranges. |
| T-03-05 | T | holdout isolation | mitigate | Emit explicit train and holdout artifacts from manifest-defined ranges and never present the trainer's random split as the canonical holdout. |
| T-03-06 | I | cleaned dataset provenance | mitigate | Write machine-readable dataset and quality sidecars that record repairs, drops, rejected windows, and per-symbol slice boundaries. |
</threat_model>

<verification>
Run `.\.venv\Scripts\python.exe -m pytest --collect-only -q tests/test_training_manifest.py tests/test_training_preparer.py`.
Run `.\.venv\Scripts\python.exe -m pytest -q tests/test_training_manifest.py tests/test_training_preparer.py -x`.
Run `node scripts/precommit-checks.mjs`.
</verification>

<success_criteria>
The repo can materialize a reproducible Phase 3 training bundle from PostgreSQL through a manifest-first contract, with explicit train/holdout boundaries, strict-versus-repair cleaning, fixed-length 640-point windows, machine-readable quality reporting, and sample-count metadata that makes downstream training viability explicit.
</success_criteria>

<output>
After completion, create `.planning/phases/03-train-the-model-on-1-minute-crypto-candles/03-02-SUMMARY.md`
</output>

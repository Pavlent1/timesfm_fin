---
phase: 03-train-the-model-on-1-minute-crypto-candles
plan: "04"
type: execute
wave: 4
depends_on:
  - "03-03"
files_modified:
  - src/training_lineage.py
  - src/compare_training_runs.py
  - AgentHelper/ProjectFiles/DescriptionFiles/src/training_lineage.py.md
  - AgentHelper/ProjectFiles/DescriptionFiles/src/compare_training_runs.py.md
  - README.md
  - tests/test_training_lineage.py
autonomous: true
requirements:
  - MODEL-01
  - OPS-01
user_setup:
  - At least one Phase 3 run directory from `03-03` exists so lineage and comparison outputs have real run-bundle inputs
  - Any referenced Phase 2 backtest run IDs remain available in PostgreSQL for summary lookup
must_haves:
  truths:
    - The operator can generate repeatable lineage and comparison outputs for two or more manual fine-tune runs instead of comparing anonymous checkpoint folders by hand.
    - Each run bundle carries real evaluation and backtest summary artifacts generated from the explicit holdout flow in 03-03, next to the checkpoint metadata required by D-17.
    - The comparison output makes dataset, holdout, and parent-checkpoint differences across runs visible so the effect of later training data additions is traceable per D-18.
  artifacts:
    - src/training_lineage.py normalizes run-bundle lineage metadata and summary artifacts for reporting.
    - src/compare_training_runs.py emits machine-readable and operator-readable comparison outputs from Phase 3 run bundles plus Phase 2 backtest metadata.
    - tests/test_training_lineage.py locks the per-run summary and cross-run comparison contract.
    - README.md documents the manual lineage/comparison reporting step for the Phase 3 workflow.
  key_links:
    - src/training_lineage.py reads the prepared-bundle and run metadata emitted by 03-02 and 03-03, including the real holdout evaluation/backtest outputs written after training, instead of inventing a second dataset ledger.
    - src/compare_training_runs.py reuses PostgreSQL backtest metadata through src/postgres_backtest.py and src/postgres_dataset.py rather than duplicating Phase 2 storage.
    - The generated comparison output binds checkpoint parentage, per-symbol date ranges, holdouts, preparer settings, and referenced backtest run IDs into one report bundle.
---

<objective>
Add the lineage and checkpoint-comparison reporting layer for Phase 3 run bundles on top of real post-train holdout evaluation/backtest outputs.

Purpose: D-17 and D-18 require more than checkpoint snapshots. Phase 3 must make each run comparison-ready by attaching real summary artifacts from 03-03 and by showing how dataset or holdout changes affected later runs.
Output: A lineage helper, a comparison/reporting CLI, workflow docs, and tests that lock the run-summary and comparison contract.
</objective>

<execution_context>
@C:/Users/Pavel/Documents/GitHub/timesfm_fin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Pavel/Documents/GitHub/timesfm_fin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-train-the-model-on-1-minute-crypto-candles/03-CONTEXT.md
@.planning/phases/03-train-the-model-on-1-minute-crypto-candles/03-RESEARCH.md
@.planning/phases/03-train-the-model-on-1-minute-crypto-candles/03-VALIDATION.md
@.planning/phases/03-train-the-model-on-1-minute-crypto-candles/03-02-PLAN.md
@.planning/phases/03-train-the-model-on-1-minute-crypto-candles/03-03-PLAN.md
@src/postgres_backtest.py
@src/postgres_dataset.py
@src/evaluation.py
@README.md

<interfaces>
From src/postgres_backtest.py:
```python
def query_backtest_step_stats(
    conn: psycopg.Connection,
    *,
    run_id: int,
) -> list[dict[str, object]]
```

From src/postgres_dataset.py:
```python
def load_postgres_settings(
    source: Mapping[str, str] | None = None,
) -> PostgresSettings

def connect_postgres(
    settings: PostgresSettings | None = None,
    *,
    autocommit: bool = True,
) -> psycopg.Connection
```

From src/evaluation.py:
```python
def restore_and_evaluate(
    model: Any,
    config: py_utils.NestedMap,
    workdir: str,
    num_classes=2,
    plus_one=True,
    horizons=[128, 64, 32, 16, 8, 4, 2],
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the lineage and comparison contract tests</name>
  <files>tests/test_training_lineage.py</files>
  <action>Create pytest coverage that defines the Phase 3 lineage/reporting contract before implementation. Lock per-run summary artifacts required by D-17, including produced checkpoint, parent checkpoint, selected symbols, per-symbol training and holdout ranges, preparer settings, copied training config identity, and real `evaluation_summary.json` and `backtest_summary.json` outputs produced after training. Add assertions that cross-run comparison output highlights dataset-slice and holdout differences per D-18, records referenced Phase 2 `backtest_run_id` values, and rejects anonymous, incomplete, or metadata-only run directories instead of producing ambiguous reports. Keep the tests collection-safe before implementation by using lazy imports or another no-import-at-collection pattern for modules that do not exist until later tasks.</action>
  <verify>
    <automated>.\.venv\Scripts\python.exe -m pytest --collect-only -q tests/test_training_lineage.py</automated>
  </verify>
  <done>The missing D-17/D-18 reporting behavior is described in executable, collection-safe tests before production code is added.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement per-run lineage and summary artifact generation</name>
  <files>src/training_lineage.py</files>
  <behavior>
    - Test 1: the helper writes per-run lineage artifacts that combine checkpoint parentage, dataset slice metadata, preparer settings, and training-config identity.
    - Test 2: real evaluation and backtest summaries from 03-03 are persisted and normalized as explicit artifacts in the run bundle rather than being synthesized from metadata alone.
    - Test 3: incomplete or contradictory run metadata is rejected before comparison output is generated.
  </behavior>
  <action>Create `src/training_lineage.py` as the canonical helper for Phase 3 run reporting. It must read the bundle and run metadata emitted by 03-02 and 03-03, validate that real holdout evaluation/backtest outputs exist for the trained checkpoint, normalize the required D-17 fields, and write comparison-ready artifacts such as `lineage_manifest.json` into each run directory. Reuse existing Phase 2 backtest metadata instead of inventing a new store, and keep the summary layer file-based or hybrid per the discretion note in `03-CONTEXT.md`. Do not synthesize `evaluation_summary.json` or `backtest_summary.json` from metadata alone; those artifacts must originate from the post-train evaluation/backtest step in 03-03 and be validated here before comparison proceeds. Create the mirrored AgentHelper description for `src/training_lineage.py` once its reporting responsibility is finalized.</action>
  <verify>
    <automated>.\.venv\Scripts\python.exe -m pytest -q tests/test_training_lineage.py -k summary -x</automated>
  </verify>
  <done>Each Phase 3 run directory can hold explicit lineage, evaluation, and backtest summary artifacts that are ready for cross-run comparison.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement the run-comparison CLI and operator docs</name>
  <files>src/compare_training_runs.py, src/training_lineage.py, README.md</files>
  <behavior>
    - Test 1: the comparison CLI accepts two or more run directories and emits machine-readable plus operator-readable comparison output.
    - Test 2: the report shows dataset, holdout, parent-checkpoint, evaluation, and backtest-run differences across runs instead of anonymous checkpoint names only.
    - Test 3: the README documents the manual report step and makes clear that comparison outputs come from run bundles plus Phase 2 backtest metadata.
  </behavior>
  <action>Create `src/compare_training_runs.py` as the manual Phase 3 reporting entrypoint. It must consume two or more run directories from 03-03, use `src/training_lineage.py` to validate and enrich their real summary artifacts, optionally resolve referenced Phase 2 backtest summaries through the existing PostgreSQL helpers, and emit at least `comparison_summary.json` plus one operator-readable report such as `comparison_summary.md` or `.txt`. Update `README.md` with the end-to-end manual Phase 3 reporting step so operators know how to produce comparison-ready outputs after each training run. Create or update the mirrored AgentHelper descriptions for `src/compare_training_runs.py` and `src/training_lineage.py` once the comparison/reporting responsibilities are stable. Keep the workflow CLI-first and manual-only per D-19/D-20.</action>
  <verify>
    <automated>.\.venv\Scripts\python.exe -m pytest -q tests/test_training_lineage.py -x</automated>
  </verify>
  <done>The repo has an explicit post-run reporting step that makes Phase 3 checkpoints comparison-ready and documents how to use it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Run-bundle manifests -> lineage helper | Previously recorded training metadata is treated as the source of truth for comparison reporting |
| PostgreSQL backtest metadata -> comparison output | External Phase 2 run facts are pulled into the operator-facing comparison summary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | T | lineage artifact generation | mitigate | Validate required D-17 fields before writing summaries and reject incomplete run bundles instead of generating partial reports. |
| T-03-11 | R | comparison summary claims | mitigate | Bind every comparison row to concrete checkpoint IDs, dataset ranges, holdout definitions, and referenced `backtest_run_id` values. |
| T-03-12 | I | Phase 2 metadata reuse | mitigate | Reuse existing PostgreSQL backtest queries and emit only the summarized fields needed for run comparison output. |
</threat_model>

<verification>
Run `.\.venv\Scripts\python.exe -m pytest --collect-only -q tests/test_training_lineage.py`.
Run `.\.venv\Scripts\python.exe -m pytest -q tests/test_training_lineage.py -x`.
Run `node scripts/precommit-checks.mjs`.
</verification>

<success_criteria>
The repo can attach comparison-ready lineage artifacts to Phase 3 run bundles, validate that each run already contains real holdout evaluation/backtest outputs, generate repeatable eval/backtest-aware comparison reports across runs, and document the manual reporting flow so later dataset additions are traceable instead of anonymous.
</success_criteria>

<output>
After completion, create `.planning/phases/03-train-the-model-on-1-minute-crypto-candles/03-04-SUMMARY.md`
</output>

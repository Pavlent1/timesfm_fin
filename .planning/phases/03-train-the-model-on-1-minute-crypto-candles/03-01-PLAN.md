---
phase: 03-train-the-model-on-1-minute-crypto-candles
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/postgres_prepare_training_source.py
  - src/postgres_ingest_binance.py
  - src/postgres_verify_data.py
  - AgentHelper/ProjectFiles/DescriptionFiles/src/postgres_prepare_training_source.py.md
  - AgentHelper/ProjectFiles/DescriptionFiles/src/postgres_ingest_binance.py.md
  - AgentHelper/ProjectFiles/DescriptionFiles/src/postgres_verify_data.py.md
  - tests/test_postgres_prepare_training_source.py
autonomous: true
requirements:
  - MODEL-01
  - MODEL-02
user_setup:
  - Local PostgreSQL service from `compose.yaml` is running and reachable from the chosen CLI runtime
  - Binance HTTP access is available for the manual source backfill command
must_haves:
  truths:
    - The operator can backfill and verify only the three Phase 3 symbols in PostgreSQL with explicit coverage targets before any training bundle is prepared.
    - The workflow exposes whether each requested symbol/range is contiguous, segmented, or missing enough data to block strict preparation.
    - PostgreSQL remains the canonical source of minute candles used by later preparation and comparison steps.
  artifacts:
    - src/postgres_prepare_training_source.py orchestrates the approved Phase 3 source backfill and verification path.
    - src/postgres_verify_data.py exposes segment and gap details that later preparation can reuse.
    - tests/test_postgres_prepare_training_source.py locks the source coverage and integrity contract.
  key_links:
    - src/postgres_prepare_training_source.py calls the existing PostgreSQL ingestion path in src/postgres_ingest_binance.py instead of inventing a second storage flow.
    - src/postgres_prepare_training_source.py consumes integrity results from src/postgres_verify_data.py before declaring a symbol/range training-ready.
    - The new source-prep contract rejects unsupported symbols so Phase 3 stays scoped to BTCUSDT, ETHUSDT, and SOLUSDT only.
---

<objective>
Close the Phase 3 source-data coverage and integrity gap in PostgreSQL before bundle preparation begins.

Purpose: The preparer cannot produce trustworthy train and holdout bundles until PostgreSQL has the required BTCUSDT, ETHUSDT, and SOLUSDT minute coverage and exposes actionable gap/segment diagnostics.
Output: A manual source-preparation CLI, richer integrity helpers, and targeted tests that define when Phase 3 data is ready for manifest-driven preparation.
</objective>

<execution_context>
@C:/Users/Pavel/Documents/GitHub/timesfm_fin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Pavel/Documents/GitHub/timesfm_fin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-train-the-model-on-1-minute-crypto-candles/03-CONTEXT.md
@.planning/phases/03-train-the-model-on-1-minute-crypto-candles/03-RESEARCH.md
@.planning/phases/03-train-the-model-on-1-minute-crypto-candles/03-VALIDATION.md
@.planning/phases/01-set-up-postgresql-database-download-and-discover-all-data-ad/01-03-SUMMARY.md
@.planning/phases/01-set-up-postgresql-database-download-and-discover-all-data-ad/01-04-SUMMARY.md
@src/postgres_ingest_binance.py
@src/postgres_verify_data.py
@src/postgres_discover_data.py
@src/postgres_dataset.py

<interfaces>
From src/postgres_ingest_binance.py:
```python
def run_ingest(
    args: argparse.Namespace,
    *,
    fetcher: Callable[..., list[list]] = fetch_binance_klines,
    now: datetime | None = None,
) -> dict[str, object]
```

From src/postgres_verify_data.py:
```python
def build_integrity_report(
    conn,
    *,
    source: str | None = None,
    symbol: str | None = None,
    timeframe: str | None = None,
    start: datetime | None = None,
    end: datetime | None = None,
) -> dict[str, object]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the Phase 3 source coverage and integrity tests</name>
  <files>tests/test_postgres_prepare_training_source.py</files>
  <action>Create targeted pytest coverage for the new Phase 3 source-preparation contract before implementation. Lock the allowed symbol scope to BTCUSDT, ETHUSDT, and SOLUSDT per D-01/D-04; assert the coverage targets of 40 months for BTCUSDT and 36 months for ETHUSDT/SOLUSDT per D-02; assert that the workflow reports whether the latest four BTC months remain reservable for backtests per D-03; and assert that long gaps and missing symbols are surfaced as blocking integrity findings rather than silently accepted. Keep the tests collection-safe before implementation by using lazy imports or another no-import-at-collection pattern for modules that do not exist until later tasks.</action>
  <verify>
    <automated>.\.venv\Scripts\python.exe -m pytest --collect-only -q tests/test_postgres_prepare_training_source.py</automated>
  </verify>
  <done>The new test file describes the exact Phase 3 source-readiness contract, remains collection-safe before implementation, and collects successfully.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the manual Phase 3 source backfill and readiness CLI</name>
  <files>src/postgres_prepare_training_source.py, src/postgres_ingest_binance.py</files>
  <behavior>
    - Test 1: unsupported symbols are rejected so the CLI stays limited to BTCUSDT, ETHUSDT, and SOLUSDT.
    - Test 2: the default Phase 3 source target maps BTCUSDT to 40 months and ETHUSDT/SOLUSDT to 36 months.
    - Test 3: readiness output shows whether the BTC default reserve needed for later backtests can still be honored.
  </behavior>
  <action>Create `src/postgres_prepare_training_source.py` as the manual Phase 3 entrypoint that orchestrates backfill and readiness checks against PostgreSQL. Reuse the existing ingestion path in `src/postgres_ingest_binance.py` rather than creating a second fetch/store stack. The CLI must stay manual-only per D-19/D-20, accept only the three approved symbols per D-01/D-04, apply the Phase 3 target coverage windows per D-02, and report source coverage in a way that preserves the later BTC holdout reserve intent per D-03. Keep PostgreSQL canonical; do not introduce any training-only CSV source of truth here. Update the mirrored AgentHelper descriptions for `src/postgres_prepare_training_source.py` and `src/postgres_ingest_binance.py` once their responsibilities are final.</action>
  <verify>
    <automated>.\.venv\Scripts\python.exe -m pytest -q tests/test_postgres_prepare_training_source.py -k source -x</automated>
  </verify>
  <done>The repo has one manual CLI for Phase 3 source backfill/readiness, and it reuses the existing PostgreSQL ingestion path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend integrity helpers for segment-aware training preflight</name>
  <files>src/postgres_verify_data.py, src/postgres_prepare_training_source.py</files>
  <behavior>
    - Test 1: integrity output distinguishes contiguous coverage from segmented coverage.
    - Test 2: long missing-minute gaps remain blocking findings for later strict preparation.
    - Test 3: the source-preparation CLI can fail fast when a requested symbol/range is not training-ready.
  </behavior>
  <action>Upgrade `src/postgres_verify_data.py` so later preparation can reuse machine-readable contiguous-segment and gap-severity details instead of only summary counts. This preflight must support the forthcoming strict-versus-repair flow per D-12/D-13 by making repairable versus non-repairable gaps explicit, but it must not perform cleaning itself yet. Wire the new details into `src/postgres_prepare_training_source.py` so Phase 3 can stop before bundle preparation when PostgreSQL coverage is incomplete or structurally unsafe. Update the mirrored AgentHelper descriptions for `src/postgres_verify_data.py` and `src/postgres_prepare_training_source.py` to reflect the new readiness and segment-analysis responsibilities.</action>
  <verify>
    <automated>.\.venv\Scripts\python.exe -m pytest -q tests/test_postgres_prepare_training_source.py -x</automated>
  </verify>
  <done>Segment-aware integrity data is available to later plans, and the Phase 3 source-preparation CLI blocks unsafe ranges before preparation starts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI source-prep inputs -> PostgreSQL ingestion helpers | Operator-selected symbols and ranges decide what remote Binance data enters the canonical store |
| PostgreSQL observations -> training readiness report | Stored data quality determines whether later preparation is safe or must halt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | T | source-prep CLI arguments | mitigate | Restrict symbols to the Phase 3 allowlist and reuse parameterized PostgreSQL helpers instead of string-built SQL. |
| T-03-02 | S/T | remote Binance coverage | mitigate | Record requested versus actual coverage through the existing ingestion-run path and fail readiness when the requested history is incomplete. |
| T-03-03 | I | integrity readiness output | mitigate | Expose segment and gap details explicitly so later preparation does not infer clean coverage from a coarse row-count summary alone. |
</threat_model>

<verification>
Run `.\.venv\Scripts\python.exe -m pytest --collect-only -q tests/test_postgres_prepare_training_source.py`.
Run `.\.venv\Scripts\python.exe -m pytest -q tests/test_postgres_prepare_training_source.py -x`.
Run `node scripts/precommit-checks.mjs`.
</verification>

<success_criteria>
The repo has one manual Phase 3 source-readiness path, it is limited to BTCUSDT/ETHUSDT/SOLUSDT, it enforces the Phase 3 coverage targets before training prep, and later plans can consume segment-aware integrity data from PostgreSQL without inventing a second canonical store.
</success_criteria>

<output>
After completion, create `.planning/phases/03-train-the-model-on-1-minute-crypto-candles/03-01-SUMMARY.md`
</output>

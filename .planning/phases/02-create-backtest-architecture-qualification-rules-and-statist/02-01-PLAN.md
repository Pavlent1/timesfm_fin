---
phase: 02-create-backtest-architecture-qualification-rules-and-statist
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backtest_metrics.py
  - tests/test_backtest_metrics.py
autonomous: true
requirements:
  - BT-03
  - BT-05
user_setup: []
must_haves:
  truths:
    - Phase 2 has one reusable implementation of normalized deviation, overshoot/undershoot classification, and signed percent deviation.
    - The locked Phase 2 metric semantics are covered by automated tests before PostgreSQL persistence or runtime migration depends on them.
  artifacts:
    - src/backtest_metrics.py exposes the Phase 2 metric helpers.
    - tests/test_backtest_metrics.py locks the overshoot/undershoot and normalized deviation formulas.
  key_links:
    - src/backtest_metrics.py defines the formulas that later PostgreSQL helpers and the runtime use unchanged.
    - tests/test_backtest_metrics.py covers both the upward and downward context-relative overshoot rules from 02-CONTEXT.md.
---

<objective>
Lock the Phase 2 metric semantics into one reusable module before storage or runtime migration begins.

Purpose: Prevent formula drift between Python runtime logic, PostgreSQL persistence, and later aggregate queries.
Output: A dedicated backtest metric helper module plus unit tests that encode the locked normalized deviation and overshoot/undershoot rules.
</objective>

<execution_context>
@C:/Users/Pavel/Documents/GitHub/timesfm_fin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Pavel/Documents/GitHub/timesfm_fin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-create-backtest-architecture-qualification-rules-and-statist/02-CONTEXT.md
@.planning/phases/02-create-backtest-architecture-qualification-rules-and-statist/02-RESEARCH.md
@.planning/phases/02-create-backtest-architecture-qualification-rules-and-statist/02-VALIDATION.md
@src/crypto_minute_backtest.py
@tests/test_crypto_minute_backtest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the Phase 2 metric contract tests</name>
  <files>tests/test_backtest_metrics.py</files>
  <read_first>.planning/phases/02-create-backtest-architecture-qualification-rules-and-statist/02-CONTEXT.md, .planning/phases/02-create-backtest-architecture-qualification-rules-and-statist/02-RESEARCH.md, tests/test_crypto_minute_backtest.py</read_first>
  <action>Create `tests/test_backtest_metrics.py` as the source-of-truth contract for the new formulas before implementation. Cover the exact normalized deviation formula `abs((predicted_close / actual_close) - 1.0) * 100`, upward and downward overshoot/undershoot classification relative to `last_input_close`, exact-match behavior producing zero deviation, and signed deviation percent where overshoot is positive and undershoot is negative. Keep the tests pure and collection-safe so later plans can import the same helpers without needing PostgreSQL or a model stub.</action>
  <verify>python -m pytest --collect-only -q tests/test_backtest_metrics.py</verify>
  <acceptance_criteria>
    - `tests/test_backtest_metrics.py` contains the literal formula string `abs((predicted_close / actual_close) - 1.0) * 100`
    - The test file contains separate upward and downward context-baseline scenarios
    - `python -m pytest --collect-only -q tests/test_backtest_metrics.py` exits successfully
  </acceptance_criteria>
  <done>The metric contract tests collect successfully and encode every locked Phase 2 formula decision.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the reusable backtest metric module</name>
  <files>src/backtest_metrics.py</files>
  <read_first>.planning/phases/02-create-backtest-architecture-qualification-rules-and-statist/02-CONTEXT.md, tests/test_backtest_metrics.py, src/crypto_minute_backtest.py</read_first>
  <action>Implement `src/backtest_metrics.py` as the single reusable home for Phase 2 metric semantics. Export concrete helpers for normalized deviation percent, signed percent deviation, overshoot/undershoot label derivation, and per-step aggregation inputs that later storage and runtime code can reuse directly. Use explicit names such as `normalized_deviation_pct`, `signed_deviation_pct`, and `classify_overshoot`, and keep the helpers free of PostgreSQL or CLI coupling. Do not leave equivalent formula code behind in `src/crypto_minute_backtest.py`; later plans should import this module instead of recomputing the rules inline.</action>
  <verify>python -m pytest -q tests/test_backtest_metrics.py -x</verify>
  <acceptance_criteria>
    - `src/backtest_metrics.py` contains functions named `normalized_deviation_pct` and `classify_overshoot`
    - `src/backtest_metrics.py` contains the string `last_input_close` in the classification path
    - `python -m pytest -q tests/test_backtest_metrics.py -x` exits successfully
  </acceptance_criteria>
  <done>The repo has one tested metric module that later plans can import without reinterpreting the Phase 2 formulas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| locked Phase 2 decisions -> executable metric code | Human decisions about semantics become code that all later storage and analytics depend on |
| metric helpers -> future SQL/runtime callers | Multiple subsystems will reuse these helpers, so ambiguity here propagates broadly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | T | Formula implementation | mitigate | Encode the exact normalized deviation and overshoot rules in tests before implementation and reuse one shared module everywhere. |
| T-02-02 | I | Signed deviation semantics | mitigate | Assert positive overshoot and negative undershoot in unit tests so later SQL/reporting code cannot silently invert meaning. |
</threat_model>

<verification>
Run `python -m pytest --collect-only -q tests/test_backtest_metrics.py`.
Run `python -m pytest -q tests/test_backtest_metrics.py -x`.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The Phase 2 metric rules live in one importable module and are pinned by dedicated tests
</success_criteria>

<output>
After completion, create `.planning/phases/02-create-backtest-architecture-qualification-rules-and-statist/02-01-SUMMARY.md`
</output>

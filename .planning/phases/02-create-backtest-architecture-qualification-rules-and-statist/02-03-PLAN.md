---
phase: 02-create-backtest-architecture-qualification-rules-and-statist
plan: "03"
type: execute
wave: 2
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - src/crypto_minute_backtest.py
  - scripts/run_crypto_backtest.ps1
  - README.md
  - db/README.md
  - tests/test_crypto_minute_backtest.py
  - tests/test_script_wrappers.py
  - tests/test_docs_contract.py
autonomous: true
requirements:
  - BT-01
  - BT-04
  - BT-05
user_setup:
  - Local PostgreSQL service from `compose.yaml` is running and reachable from the chosen runtime path
  - The Phase 1 dataset contains the requested `binance` / `BTCUSDT` / `1m` candles, or the runtime can persist freshly fetched live candles into PostgreSQL first
must_haves:
  truths:
    - The crypto backtest runtime reads and writes PostgreSQL-backed data through the shared repo path instead of using SQLite as a canonical store.
    - The Docker wrapper passes the PostgreSQL connection settings needed for containerized runs.
    - Repository docs describe the PostgreSQL-backed backtest workflow and remove SQLite as the primary persistence recommendation.
  artifacts:
    - src/crypto_minute_backtest.py uses the shared metric and PostgreSQL helper modules.
    - scripts/run_crypto_backtest.ps1 injects PostgreSQL connection env vars into `docker run`.
    - README.md and db/README.md document the Phase 2 backtest path.
    - tests/test_crypto_minute_backtest.py, tests/test_script_wrappers.py, and tests/test_docs_contract.py verify the migrated contracts.
  key_links:
    - src/crypto_minute_backtest.py imports src/backtest_metrics.py and src/postgres_backtest.py instead of defining SQLite DDL inline.
    - scripts/run_crypto_backtest.ps1 passes `POSTGRES_HOST=host.docker.internal` and compatible Docker host wiring for containerized runs.
    - README.md and db/README.md point operators to PostgreSQL-backed storage and the step-stats view rather than `outputs/crypto_backtest.sqlite`.
---

<objective>
Migrate the crypto minute backtest runtime, wrapper, and docs to the PostgreSQL-backed Phase 2 architecture.

Purpose: Finish the Phase 2 user-visible path so operators run one canonical database-backed backtest workflow instead of a split PostgreSQL-plus-SQLite model.
Output: A PostgreSQL-backed crypto backtest runtime, updated Docker wrapper wiring, and docs/tests that describe the new workflow clearly.
</objective>

<execution_context>
@C:/Users/Pavel/Documents/GitHub/timesfm_fin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Pavel/Documents/GitHub/timesfm_fin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-create-backtest-architecture-qualification-rules-and-statist/02-CONTEXT.md
@.planning/phases/02-create-backtest-architecture-qualification-rules-and-statist/02-RESEARCH.md
@.planning/phases/02-create-backtest-architecture-qualification-rules-and-statist/02-VALIDATION.md
@.planning/phases/02-create-backtest-architecture-qualification-rules-and-statist/02-01-PLAN.md
@.planning/phases/02-create-backtest-architecture-qualification-rules-and-statist/02-02-PLAN.md
@src/crypto_minute_backtest.py
@scripts/run_crypto_backtest.ps1
@README.md
@db/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the runtime, wrapper, and docs contract tests</name>
  <files>tests/test_crypto_minute_backtest.py, tests/test_script_wrappers.py, tests/test_docs_contract.py</files>
  <read_first>.planning/phases/02-create-backtest-architecture-qualification-rules-and-statist/02-CONTEXT.md, .planning/phases/02-create-backtest-architecture-qualification-rules-and-statist/02-RESEARCH.md, tests/test_crypto_minute_backtest.py, tests/test_script_wrappers.py, tests/test_docs_contract.py</read_first>
  <action>Update the existing test suite so it describes the PostgreSQL-backed Phase 2 behavior before implementation. Replace SQLite-specific assumptions with PostgreSQL helper integration, assert that runtime persistence writes run/window/step facts, assert that the PowerShell wrapper passes PostgreSQL env vars into the container, and make the docs contract fail if README or `db/README.md` still present SQLite as the canonical backtest store. Keep the tests deterministic by stubbing model output and database helpers instead of requiring live TimesFM downloads.</action>
  <verify>python -m pytest --collect-only -q tests/test_crypto_minute_backtest.py tests/test_script_wrappers.py tests/test_docs_contract.py</verify>
  <acceptance_criteria>
    - `tests/test_script_wrappers.py` contains `POSTGRES_HOST`
    - `tests/test_docs_contract.py` checks for PostgreSQL-backed backtest commands or docs wording
    - `python -m pytest --collect-only -q tests/test_crypto_minute_backtest.py tests/test_script_wrappers.py tests/test_docs_contract.py` exits successfully
  </acceptance_criteria>
  <done>The existing contract tests now describe the Phase 2 PostgreSQL runtime path instead of the old SQLite behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate the runtime and wrapper to PostgreSQL</name>
  <files>src/crypto_minute_backtest.py, scripts/run_crypto_backtest.ps1</files>
  <read_first>src/backtest_metrics.py, src/postgres_backtest.py, src/postgres_dataset.py, tests/test_crypto_minute_backtest.py, tests/test_script_wrappers.py</read_first>
  <action>Refactor `src/crypto_minute_backtest.py` so it imports `src/backtest_metrics.py` and `src/postgres_backtest.py` instead of declaring SQLite schema and storage inline. For backtest mode, load source candles from Phase 1 PostgreSQL tables for `source_name='binance'`, `timeframe='1m'`, and the requested symbol/day; for live mode, persist freshly fetched candles into PostgreSQL before forecasting so the database stays canonical. Remove `--db-path` as the primary storage flag, keep CLI-first behavior, and record enough run metadata to satisfy BT-04. Update `scripts/run_crypto_backtest.ps1` so containerized runs pass `POSTGRES_HOST=host.docker.internal`, `POSTGRES_PORT=54329`, and `--add-host=host.docker.internal:host-gateway`, while preserving the existing tuning flags and image/build behavior.</action>
  <verify>python -m pytest -q tests/test_crypto_minute_backtest.py tests/test_script_wrappers.py -x</verify>
  <acceptance_criteria>
    - `src/crypto_minute_backtest.py` no longer imports `sqlite3`
    - `src/crypto_minute_backtest.py` imports `backtest_metrics` and `postgres_backtest`
    - `scripts/run_crypto_backtest.ps1` contains `POSTGRES_HOST` and `host.docker.internal`
    - `python -m pytest -q tests/test_crypto_minute_backtest.py tests/test_script_wrappers.py -x` exits successfully
  </acceptance_criteria>
  <done>The runtime and wrapper use PostgreSQL as the canonical backtest store and the old SQLite persistence path is removed from the main workflow.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update the operator docs for the PostgreSQL backtest flow</name>
  <files>README.md, db/README.md</files>
  <read_first>README.md, db/README.md, tests/test_docs_contract.py, scripts/run_crypto_backtest.ps1</read_first>
  <action>Rewrite the backtest documentation to describe the PostgreSQL-backed Phase 2 path explicitly. Replace the `outputs/crypto_backtest.sqlite` examples with commands that assume the Phase 1 PostgreSQL service is the canonical store, document how the wrapper reaches PostgreSQL from the container, and show how to inspect `market_data.backtest_step_stats_vw` or equivalent documented query output for per-step statistics. Keep the scope to CLI/operator guidance; do not introduce a dashboard or service-oriented story.</action>
  <verify>python -m pytest -q tests/test_docs_contract.py -x</verify>
  <acceptance_criteria>
    - `README.md` contains `PostgreSQL` in the crypto backtest section
    - `README.md` does not contain the exact string `outputs/crypto_backtest.sqlite`
    - `db/README.md` contains `backtest_step_stats_vw` or the final documented per-step stats view name
    - `python -m pytest -q tests/test_docs_contract.py -x` exits successfully
  </acceptance_criteria>
  <done>The docs present one PostgreSQL-backed backtest path and explain where per-step stats live.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI and Docker wrapper -> PostgreSQL service | Operator commands and container wiring control whether results land in the canonical database |
| repository docs -> operator workflow | README and database docs instruct the user which storage path is authoritative |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | R | Runtime provenance | mitigate | Persist run metadata, source coverage, and settings through the shared PostgreSQL helpers instead of transient SQLite files. |
| T-02-06 | D | Docker wrapper networking | mitigate | Pass explicit PostgreSQL env vars plus `host.docker.internal` host wiring so containerized runs can reach the compose-managed database predictably. |
| T-02-07 | I | Operator documentation | mitigate | Enforce PostgreSQL wording and command examples through docs contract tests so SQLite does not remain as stale guidance. |
</threat_model>

<verification>
Run `python -m pytest --collect-only -q tests/test_crypto_minute_backtest.py tests/test_script_wrappers.py tests/test_docs_contract.py`.
Run `python -m pytest -q tests/test_crypto_minute_backtest.py tests/test_script_wrappers.py tests/test_docs_contract.py -x`.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The runtime, wrapper, and docs all point to PostgreSQL as the canonical backtest storage path
</success_criteria>

<output>
After completion, create `.planning/phases/02-create-backtest-architecture-qualification-rules-and-statist/02-03-SUMMARY.md`
</output>

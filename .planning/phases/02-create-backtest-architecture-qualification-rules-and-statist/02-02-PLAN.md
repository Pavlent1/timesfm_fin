---
phase: 02-create-backtest-architecture-qualification-rules-and-statist
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - db/init/002_phase2_backtest_schema.sql
  - src/postgres_backtest.py
  - src/bootstrap_postgres.py
  - src/postgres_dataset.py
  - tests/test_postgres_backtest.py
autonomous: true
requirements:
  - BT-01
  - BT-02
  - BT-04
  - BT-05
user_setup: []
must_haves:
  truths:
    - PostgreSQL can store canonical backtest run, window, and step facts without falling back to SQLite.
    - A queryable PostgreSQL view exposes per-step aggregate stats for a run.
    - The repo bootstrap path applies the new Phase 2 schema alongside the Phase 1 schema.
  artifacts:
    - db/init/002_phase2_backtest_schema.sql defines the Phase 2 backtest tables and stats view.
    - src/postgres_backtest.py provides shared save/load/query helpers for backtest artifacts.
    - tests/test_postgres_backtest.py verifies schema application and row-level persistence.
  key_links:
    - src/bootstrap_postgres.py applies db/init/002_phase2_backtest_schema.sql through the same checked-in bootstrap path used in Phase 1.
    - src/postgres_backtest.py writes rows that `market_data.backtest_step_stats_vw` can aggregate by run and step index.
---

<objective>
Extend the PostgreSQL foundation with the Phase 2 backtest schema and reusable storage helpers.

Purpose: Give the runtime a canonical database contract for run/window/step persistence and direct per-horizon analytics.
Output: Checked-in Phase 2 schema SQL, bootstrap support for multi-file schema application, and a shared PostgreSQL backtest helper module with contract tests.
</objective>

<execution_context>
@C:/Users/Pavel/Documents/GitHub/timesfm_fin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Pavel/Documents/GitHub/timesfm_fin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-create-backtest-architecture-qualification-rules-and-statist/02-CONTEXT.md
@.planning/phases/02-create-backtest-architecture-qualification-rules-and-statist/02-RESEARCH.md
@.planning/phases/02-create-backtest-architecture-qualification-rules-and-statist/02-VALIDATION.md
@src/postgres_dataset.py
@src/bootstrap_postgres.py
@db/init/001_phase1_schema.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the PostgreSQL schema and helper contract tests</name>
  <files>tests/test_postgres_backtest.py</files>
  <read_first>.planning/phases/02-create-backtest-architecture-qualification-rules-and-statist/02-CONTEXT.md, .planning/phases/02-create-backtest-architecture-qualification-rules-and-statist/02-RESEARCH.md, src/postgres_dataset.py, src/bootstrap_postgres.py</read_first>
  <action>Create `tests/test_postgres_backtest.py` before implementation. Cover three behaviors explicitly: multi-file bootstrap applies the new Phase 2 SQL after Phase 1 bootstrap, saving a backtest writes one row to `market_data.backtest_runs`, one row per forecast origin to `market_data.backtest_windows`, and one row per horizon step to `market_data.backtest_prediction_steps`, and `market_data.backtest_step_stats_vw` exposes grouped step statistics for one run. Use deterministic fixtures and PostgreSQL-only assertions; do not use SQLite compatibility shims in these tests.</action>
  <verify>python -m pytest --collect-only -q tests/test_postgres_backtest.py</verify>
  <acceptance_criteria>
    - `tests/test_postgres_backtest.py` references `market_data.backtest_prediction_steps`
    - `tests/test_postgres_backtest.py` references `market_data.backtest_step_stats_vw`
    - `python -m pytest --collect-only -q tests/test_postgres_backtest.py` exits successfully
  </acceptance_criteria>
  <done>The PostgreSQL contract tests collect cleanly and define the Phase 2 storage/view behavior before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the Phase 2 PostgreSQL schema and bootstrap path</name>
  <files>db/init/002_phase2_backtest_schema.sql, src/bootstrap_postgres.py, src/postgres_dataset.py</files>
  <read_first>db/init/001_phase1_schema.sql, tests/test_postgres_backtest.py, src/postgres_dataset.py, src/bootstrap_postgres.py</read_first>
  <action>Add `db/init/002_phase2_backtest_schema.sql` with concrete objects `market_data.backtest_runs`, `market_data.backtest_windows`, `market_data.backtest_prediction_steps`, and `market_data.backtest_step_stats_vw`. The step table must include run identity, window identity, `step_index`, `target_time_utc`, `last_input_close`, `predicted_close`, `actual_close`, `normalized_deviation_pct`, `signed_deviation_pct`, and `overshoot_label`. Update the bootstrap path so it applies all checked-in schema SQL files in lexical order instead of assuming only `001_phase1_schema.sql`; keep the checked-in SQL files as the only schema source of truth.</action>
  <verify>python -m pytest -q tests/test_postgres_backtest.py -k "bootstrap or schema" -x</verify>
  <acceptance_criteria>
    - `db/init/002_phase2_backtest_schema.sql` contains `CREATE TABLE IF NOT EXISTS market_data.backtest_prediction_steps`
    - `db/init/002_phase2_backtest_schema.sql` contains `CREATE VIEW market_data.backtest_step_stats_vw` or `CREATE OR REPLACE VIEW market_data.backtest_step_stats_vw`
    - `src/postgres_dataset.py` or `src/bootstrap_postgres.py` contains logic that iterates over `db/init/*.sql` in order
  </acceptance_criteria>
  <done>The checked-in PostgreSQL bootstrap path can create the Phase 2 schema and view without manual SQL steps.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement the shared PostgreSQL backtest helper module</name>
  <files>src/postgres_backtest.py</files>
  <read_first>tests/test_postgres_backtest.py, src/postgres_dataset.py, .planning/phases/02-create-backtest-architecture-qualification-rules-and-statist/02-01-PLAN.md</read_first>
  <action>Create `src/postgres_backtest.py` with explicit helper functions for creating a run record, creating a window record, inserting batched step rows, and querying the per-step stats view for one run. Use direct `psycopg` parameterized SQL following the repo's existing PostgreSQL helper style, return generated IDs through `RETURNING`, and keep the module independent from CLI parsing or Binance fetching. Do not duplicate metric formulas here; callers must pass the already-computed Phase 2 metric fields from `src/backtest_metrics.py`.</action>
  <verify>python -m pytest -q tests/test_postgres_backtest.py -x</verify>
  <acceptance_criteria>
    - `src/postgres_backtest.py` contains functions named `create_backtest_run`, `create_backtest_window`, and `insert_backtest_steps`
    - `src/postgres_backtest.py` uses `%s` parameter placeholders instead of string-built SQL
    - `python -m pytest -q tests/test_postgres_backtest.py -x` exits successfully
  </acceptance_criteria>
  <done>The repo has reusable PostgreSQL helpers that the runtime can call for backtest persistence and step-stat queries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime metric rows -> PostgreSQL tables | Application code writes durable experimental records into the canonical database |
| checked-in schema SQL -> live PostgreSQL instance | Bootstrap files define the durable storage contract for later runtime code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | T | Backtest tables and view | mitigate | Keep schema in checked-in SQL and verify exact tables/view names through contract tests before runtime migration. |
| T-02-04 | R | Run provenance fields | mitigate | Require explicit run metadata columns plus helper functions that return inserted IDs and preserve source coverage fields. |
| T-02-05 | I | SQL helper layer | mitigate | Use parameterized `psycopg` queries only and forbid string-built SQL with user-supplied values. |
</threat_model>

<verification>
Run `python -m pytest --collect-only -q tests/test_postgres_backtest.py`.
Run `python -m pytest -q tests/test_postgres_backtest.py -x`.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- PostgreSQL has a checked-in Phase 2 schema, reusable helper layer, and direct per-step stats view
</success_criteria>

<output>
After completion, create `.planning/phases/02-create-backtest-architecture-qualification-rules-and-statist/02-02-SUMMARY.md`
</output>

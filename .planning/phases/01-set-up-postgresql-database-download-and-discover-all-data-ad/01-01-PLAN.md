---
phase: 01-set-up-postgresql-database-download-and-discover-all-data-ad
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - compose.yaml
  - db/init/001_phase1_schema.sql
  - src/postgres_dataset.py
  - src/bootstrap_postgres.py
  - requirements.inference.txt
  - requirements.dev.txt
  - tests/conftest.py
  - tests/test_db_connection.py
  - tests/test_schema_bootstrap.py
autonomous: true
requirements:
  - DB-01
  - DB-02
  - DB-03
must_haves:
  truths:
    - User can start a repo-owned PostgreSQL service and connect to it from project code.
    - User can create the Phase 1 schema without manual SQL edits.
    - The schema is ready for multi-asset 1-minute close-price storage.
  artifacts:
    - compose.yaml defines the pinned PostgreSQL service.
    - db/init/001_phase1_schema.sql creates assets, series, ingestion_runs, and observations.
    - src/postgres_dataset.py and src/bootstrap_postgres.py expose the shared connection and bootstrap path.
    - tests/test_db_connection.py and tests/test_schema_bootstrap.py verify the foundation contract.
  key_links:
    - compose.yaml mounts db/init into docker-entrypoint-initdb.d on first boot.
    - src/bootstrap_postgres.py imports the shared DSN helpers from src/postgres_dataset.py.
    - observations stores close_price as double precision behind a uniqueness key later plans can upsert against.
---

<objective>
Establish the repository-owned PostgreSQL foundation for Phase 1.

Purpose: Deliver the single supported local database path and schema bootstrap that every later Phase 1 plan depends on.
Output: Compose-backed PostgreSQL setup, shared Python connection/bootstrap helpers, and the first pytest-based database contract tests.
</objective>

<execution_context>
@C:/Users/Pavel/Documents/GitHub/timesfm_fin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Pavel/Documents/GitHub/timesfm_fin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-set-up-postgresql-database-download-and-discover-all-data-ad/01-CONTEXT.md
@.planning/phases/01-set-up-postgresql-database-download-and-discover-all-data-ad/01-RESEARCH.md
@.planning/phases/01-set-up-postgresql-database-download-and-discover-all-data-ad/01-VALIDATION.md
@README.md
@src/crypto_minute_backtest.py

<interfaces>
From src/crypto_minute_backtest.py:
```python
def ensure_schema(conn: sqlite3.Connection) -> None: ...
def load_candles(
    conn: sqlite3.Connection,
    exchange: str,
    symbol: str,
    interval: str,
    start_ms: int,
    end_ms: int,
) -> pd.DataFrame: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the Phase 1 PostgreSQL test harness</name>
  <files>requirements.dev.txt, tests/conftest.py, tests/test_db_connection.py, tests/test_schema_bootstrap.py</files>
  <action>Create the first pytest scaffolding for Phase 1 per 01-VALIDATION.md. Add pytest to a new repo-local development requirements file, define reusable fixtures in tests/conftest.py for the repo compose service and isolated database setup, and write collection-safe tests that pin the required connection and schema behavior before implementation. Keep the fixtures generic enough for later ingestion, discovery, and materialization plans so same-wave plans do not need to modify conftest.py.</action>
  <verify>
    <automated>python -m pytest --collect-only -q tests/test_db_connection.py tests/test_schema_bootstrap.py</automated>
  </verify>
  <done>Pytest collects the new files, and the fixture/test contract clearly defines the compose service, connection path, and schema objects the implementation must satisfy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the compose-backed PostgreSQL foundation</name>
  <files>compose.yaml, db/init/001_phase1_schema.sql, src/postgres_dataset.py, src/bootstrap_postgres.py, requirements.inference.txt</files>
  <behavior>
    - Test 1: project code can connect to the compose-managed PostgreSQL service through one repo-owned path per D-01 and D-02.
    - Test 2: schema bootstrap creates assets, series, ingestion_runs, and observations without manual SQL edits per DB-02 and D-03.
    - Test 3: observations stores 1-minute close prices as double precision with a uniqueness key that enables later idempotent upserts per D-03, D-04, D-06, and the research default.
  </behavior>
  <action>Implement a root compose.yaml using the pinned official postgres:18.3-bookworm image per research and D-01, mount db/init into docker-entrypoint-initdb.d, and keep the service aligned to one documented local runtime path per D-02. Add db/init/001_phase1_schema.sql that models assets, logical series metadata, ingestion_runs provenance, and observations for multi-asset 1-minute close-price storage per D-03, D-04, D-06, and DB-03; use double precision for close_price per the resolved research default. Add psycopg[binary]==3.3.3 to requirements.inference.txt, then create src/postgres_dataset.py and src/bootstrap_postgres.py so later plans reuse shared DSN/bootstrap helpers instead of ad hoc SQL or ORM tooling. Do not introduce SQLAlchemy, Alembic, or manual one-off SQL instructions.</action>
  <verify>
    <automated>python -m pytest -q tests/test_db_connection.py tests/test_schema_bootstrap.py -x</automated>
  </verify>
  <done>The repository has a reproducible PostgreSQL service, Python can connect to it through shared helpers, and the Phase 1 schema is created without manual SQL editing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host CLI -> docker compose postgres | Local operator input controls service startup and bootstrap commands |
| Python runtime -> PostgreSQL | Application code sends schema/bootstrap SQL and later query traffic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T | compose.yaml + bootstrap SQL | mitigate | Keep schema/bootstrap in checked-in files, use pytest contract tests, and forbid ad hoc manual SQL steps in task actions. |
| T-01-02 | I | DSN and local credentials | mitigate | Centralize connection settings in src/postgres_dataset.py, allow env overrides, and document the local-only trust boundary in Phase 1 docs. |
| T-01-03 | E | Database bootstrap privileges | mitigate | Limit bootstrap logic to schema creation helpers and plan for later commands to reuse the shared app connection path instead of superuser-only shell access. |
</threat_model>

<verification>
Run `python -m pytest -q tests/test_db_connection.py tests/test_schema_bootstrap.py`.
Run `docker compose config` to confirm the checked-in service definition is syntactically valid.
</verification>

<success_criteria>
The repo owns one Docker-based PostgreSQL setup path, the Phase 1 schema exists without manual SQL edits, and later plans can import a shared connection/bootstrap module instead of inventing their own database wiring.
</success_criteria>

<output>
After completion, create `.planning/phases/01-set-up-postgresql-database-download-and-discover-all-data-ad/01-01-SUMMARY.md`
</output>

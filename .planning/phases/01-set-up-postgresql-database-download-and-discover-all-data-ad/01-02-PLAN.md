---
phase: 01-set-up-postgresql-database-download-and-discover-all-data-ad
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/binance_market_data.py
  - src/postgres_ingest_binance.py
  - src/crypto_minute_backtest.py
  - tests/test_binance_ingest.py
  - tests/test_provenance.py
autonomous: true
requirements:
  - DB-03
  - ING-01
  - ING-02
  - ING-03
must_haves:
  truths:
    - User can run one repeatable command to ingest about one year of BTCUSDT 1-minute close-price data into PostgreSQL.
    - Re-running the same ingest does not create duplicate observations or silently corrupt stored rows.
    - Every ingest run records source, load time, and coverage metadata.
  artifacts:
    - src/binance_market_data.py owns the reusable Binance pagination and UTC normalization logic.
    - src/postgres_ingest_binance.py exposes the one-shot PostgreSQL ingestion CLI.
    - tests/test_binance_ingest.py and tests/test_provenance.py lock idempotence and provenance behavior.
  key_links:
    - src/postgres_ingest_binance.py reuses src/postgres_dataset.py from 01-01 for its PostgreSQL connection path.
    - src/crypto_minute_backtest.py imports the extracted Binance helper instead of carrying a second pagination implementation.
    - ingestion writes both observations and ingestion_runs in one tracked workflow.
---

<objective>
Build the Phase 1 market-data ingestion path and provenance tracking.

Purpose: Turn the new PostgreSQL schema into a repeatable data-loading workflow that satisfies the first concrete BTCUSDT ingestion requirement without painting the repo into a BTC-only corner.
Output: Reusable Binance fetch helper, idempotent PostgreSQL ingestion CLI, provenance recording, and regression tests for reruns.
</objective>

<execution_context>
@C:/Users/Pavel/Documents/GitHub/timesfm_fin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Pavel/Documents/GitHub/timesfm_fin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-set-up-postgresql-database-download-and-discover-all-data-ad/01-CONTEXT.md
@.planning/phases/01-set-up-postgresql-database-download-and-discover-all-data-ad/01-RESEARCH.md
@.planning/phases/01-set-up-postgresql-database-download-and-discover-all-data-ad/01-VALIDATION.md
@src/crypto_minute_backtest.py
@.planning/phases/01-set-up-postgresql-database-download-and-discover-all-data-ad/01-01-PLAN.md

<interfaces>
From src/crypto_minute_backtest.py:
```python
BINANCE_KLINES_URL = "https://api.binance.com/api/v3/klines"
ONE_MINUTE_MS = 60_000
def fetch_binance_klines(
    symbol: str,
    start_ms: int,
    end_ms: int,
    interval: str = "1m",
    limit: int = 1000,
) -> list[list]: ...
def to_utc_iso(timestamp_ms: int) -> str: ...
```

Expected from src/postgres_dataset.py after 01-01:
```python
def connect_postgres(...): ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write ingestion and provenance regression tests</name>
  <files>tests/test_binance_ingest.py, tests/test_provenance.py</files>
  <action>Create deterministic pytest coverage for the ingestion CLI before implementation. Stub Binance responses so the tests do not depend on live network calls, cover the default BTCUSDT 1-minute one-year workflow per D-05, and pin idempotent reruns plus provenance fields per D-07, ING-02, and ING-03. Reuse the shared PostgreSQL fixtures from 01-01 instead of modifying conftest.py.</action>
  <verify>
    <automated>python -m pytest --collect-only -q tests/test_binance_ingest.py tests/test_provenance.py</automated>
  </verify>
  <done>The new test files collect cleanly and encode the default ingest command, idempotence rules, and provenance fields the implementation must satisfy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement idempotent Binance ingestion into PostgreSQL</name>
  <files>src/binance_market_data.py, src/postgres_ingest_binance.py, src/crypto_minute_backtest.py</files>
  <behavior>
    - Test 1: the default CLI ingests about 365 days of BTCUSDT 1-minute close-price data into the Phase 1 schema per D-04 and D-05.
    - Test 2: rerunning the same range keeps one observation per series/timestamp and does not silently corrupt existing rows per D-07 and ING-02.
    - Test 3: each run records source endpoint, requested range, actual stored range, row counts, and completion metadata per ING-03.
  </behavior>
  <action>Extract the existing Binance pagination and UTC helpers from src/crypto_minute_backtest.py into src/binance_market_data.py so both the legacy SQLite workflow and the new PostgreSQL workflow use one implementation. Add src/postgres_ingest_binance.py as the one-shot CLI that defaults to BTCUSDT, timeframe 1m, and roughly the last 365 days per D-05 while still accepting symbol/source/timeframe parameters so the schema remains multi-asset ready per D-03 and D-06. Use the connection helpers from 01-01, create or reuse logical series rows, upsert observations with INSERT ... ON CONFLICT against the schema uniqueness key, and persist ingestion_runs metadata capturing provenance and coverage per D-07. Keep the workflow batch-only; do not add scheduling or streaming behavior that was explicitly deferred.</action>
  <verify>
    <automated>python -m pytest -q tests/test_binance_ingest.py tests/test_provenance.py -x</automated>
  </verify>
  <done>The repo can load the initial Binance dataset into PostgreSQL through one repeatable command, reruns stay idempotent, and provenance metadata is queryable for later discovery tools.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Binance API -> ingestion CLI | Untrusted remote market data enters the repo through HTTP responses |
| ingestion CLI -> PostgreSQL | Loader code writes time-series rows and provenance metadata into the database |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | T | src/postgres_ingest_binance.py | mitigate | Validate symbol/timeframe/date inputs at the CLI boundary, normalize timestamps to UTC, and use ON CONFLICT upserts instead of delete-reinsert behavior. |
| T-01-05 | D | Binance pagination workflow | mitigate | Reuse a single pagination helper, detect stalled cursors, and surface partial-fetch failures instead of silently accepting truncated data. |
| T-01-06 | R | ingestion_runs provenance | mitigate | Persist requested range, actual stored range, row counts, and status so each ingest is auditable after the fact. |
</threat_model>

<verification>
Run `python -m pytest -q tests/test_binance_ingest.py tests/test_provenance.py`.
Smoke-test the CLI help with `python src/postgres_ingest_binance.py --help`.
</verification>

<success_criteria>
The repository has a reusable Binance downloader wired into PostgreSQL ingestion, the initial BTCUSDT load is repeatable, and provenance plus coverage metadata exist for every ingest run.
</success_criteria>

<output>
After completion, create `.planning/phases/01-set-up-postgresql-database-download-and-discover-all-data-ad/01-02-SUMMARY.md`
</output>

---
phase: 01-set-up-postgresql-database-download-and-discover-all-data-ad
plan: "03"
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - src/postgres_discover_data.py
  - src/postgres_verify_data.py
  - tests/test_discovery_cli.py
autonomous: true
requirements:
  - DISC-01
  - DISC-02
must_haves:
  truths:
    - User can inspect which symbols, sources, timeframes, and date ranges exist in PostgreSQL through project commands.
    - User can sort and filter discovery results by symbol, source, timeframe, and date range.
    - User can run an integrity report that surfaces duplicates, minute gaps, ordering issues, nulls, out-of-range timestamps, and coverage summaries.
  artifacts:
    - src/postgres_discover_data.py provides the inventory and sorting CLI.
    - src/postgres_verify_data.py provides the integrity verification CLI.
    - tests/test_discovery_cli.py locks discovery filters, sorting, and integrity output.
  key_links:
    - Both CLIs reuse the shared PostgreSQL connection path from 01-01.
    - Integrity verification reads the same observations and ingestion provenance written by 01-02.
---

<objective>
Deliver script-driven discovery, sorting, and integrity verification for the PostgreSQL dataset.

Purpose: Make the stored dataset inspectable without manual SQL while enforcing the cleanliness checks the user locked for Phase 1.
Output: Discovery and integrity CLIs plus automated regression coverage for filtering, ordering, and gap detection.
</objective>

<execution_context>
@C:/Users/Pavel/Documents/GitHub/timesfm_fin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Pavel/Documents/GitHub/timesfm_fin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-set-up-postgresql-database-download-and-discover-all-data-ad/01-CONTEXT.md
@.planning/phases/01-set-up-postgresql-database-download-and-discover-all-data-ad/01-RESEARCH.md
@.planning/phases/01-set-up-postgresql-database-download-and-discover-all-data-ad/01-VALIDATION.md
@.planning/phases/01-set-up-postgresql-database-download-and-discover-all-data-ad/01-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write discovery and integrity CLI tests</name>
  <files>tests/test_discovery_cli.py</files>
  <action>Create a single targeted pytest file that exercises both the inventory CLI and the integrity CLI against seeded PostgreSQL data from the earlier fixtures. Cover filters and sort order for source, symbol, timeframe, and date range per D-11 and DISC-02, and encode integrity expectations for duplicate timestamps, missing minute gaps, ordering problems, null values, out-of-range timestamps, and coverage summaries per D-09 and D-10. Keep the test file collection-safe before the production modules exist.</action>
  <verify>
    <automated>python -m pytest --collect-only -q tests/test_discovery_cli.py</automated>
  </verify>
  <done>The discovery test file collects cleanly and defines the exact report/filter contract the implementation must meet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement dataset discovery and integrity reporting</name>
  <files>src/postgres_discover_data.py, src/postgres_verify_data.py</files>
  <behavior>
    - Test 1: discovery output lists available dataset slices with source, symbol, timeframe, row counts, and coverage range per DISC-01 and D-08.
    - Test 2: discovery supports filtering and deterministic sorting by source, symbol, timeframe, and date range per D-11 and DISC-02.
    - Test 3: integrity verification reports duplicate timestamps, missing minute gaps, ordering issues, nulls, out-of-range timestamps, and coverage summaries per D-09 and D-10.
  </behavior>
  <action>Implement src/postgres_discover_data.py and src/postgres_verify_data.py as script-driven PostgreSQL CLIs that reuse the shared connection path from 01-01 and the observation/provenance tables from 01-02. The discovery command must expose inventory views and filter/sort flags so users can inspect stored data without writing SQL per D-08 and D-11. The integrity command must compute exactly the locked checks from D-10 and print human-readable summaries suitable for CLI operators. Use parameterized SQL only; do not string-build user-provided filters into raw SQL.</action>
  <verify>
    <automated>python -m pytest -q tests/test_discovery_cli.py -x</automated>
  </verify>
  <done>Users can inspect and sort the PostgreSQL dataset from project commands, and the repo can automatically detect the integrity conditions the phase context requires.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI filters -> SQL queries | Untrusted filter and sort input shapes discovery and integrity queries |
| PostgreSQL -> CLI reports | Database contents are rendered into operator-facing summaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | T | discovery filter handling | mitigate | Use parameterized queries and an allowlist for sort keys instead of interpolating raw CLI values into SQL. |
| T-01-08 | D | unbounded discovery queries | mitigate | Default to aggregated reports, require explicit row-detail flags if added later, and keep date filters available so large datasets remain explorable. |
| T-01-09 | I | integrity output | accept | Reports are local operator tooling over local development data; document that Phase 1 discovery is not a public-facing endpoint. |
</threat_model>

<verification>
Run `python -m pytest -q tests/test_discovery_cli.py`.
Smoke-test the command help for `python src/postgres_discover_data.py --help` and `python src/postgres_verify_data.py --help`.
</verification>

<success_criteria>
The PostgreSQL dataset is inspectable through repo-native commands, filter and sort behavior is deterministic, and integrity verification catches the exact data-quality failures called out in the phase decisions.
</success_criteria>

<output>
After completion, create `.planning/phases/01-set-up-postgresql-database-download-and-discover-all-data-ad/01-03-SUMMARY.md`
</output>

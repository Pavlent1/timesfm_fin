---
phase: 01-set-up-postgresql-database-download-and-discover-all-data-ad
plan: "04"
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - src/postgres_materialize_dataset.py
  - README.md
  - db/README.md
  - tests/test_materialize_dataset.py
  - tests/test_docs_contract.py
autonomous: true
requirements:
  - DISC-03
must_haves:
  truths:
    - User can export PostgreSQL data to CSV in a format existing Phase 1-era workflows can consume.
    - User can export a training-oriented CSV bridge without hand-editing database output.
    - User can understand the schema, setup path, ingestion flow, discovery commands, and materialization workflow from repository docs.
  artifacts:
    - src/postgres_materialize_dataset.py provides the PostgreSQL-to-CSV bridge.
    - README.md and db/README.md document the setup, schema, ingestion, discovery, verification, and materialization workflow.
    - tests/test_materialize_dataset.py and tests/test_docs_contract.py lock bridge and documentation behavior.
  key_links:
    - Materialized series CSVs align with src/run_forecast.py and src/evaluate_forecast.py input expectations.
    - Training-matrix output aligns with src/train.py preprocess_csv() without manual reshaping.
    - Docs describe the single repo-native setup path established in 01-01 and the ingest/discovery commands from 01-02 and 01-03.
---

<objective>
Finish Phase 1 with the PostgreSQL-to-CSV bridge and operator documentation.

Purpose: Satisfy the locked requirement for a working downstream path into the existing CSV-driven workflows while documenting the new data layer clearly enough for later phases to build on.
Output: Materialization CLI, compatibility tests for current consumers, and repository docs that explain the Phase 1 schema and workflow end to end.
</objective>

<execution_context>
@C:/Users/Pavel/Documents/GitHub/timesfm_fin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Pavel/Documents/GitHub/timesfm_fin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-set-up-postgresql-database-download-and-discover-all-data-ad/01-CONTEXT.md
@.planning/phases/01-set-up-postgresql-database-download-and-discover-all-data-ad/01-RESEARCH.md
@.planning/phases/01-set-up-postgresql-database-download-and-discover-all-data-ad/01-VALIDATION.md
@README.md
@src/run_forecast.py
@src/evaluate_forecast.py
@src/train.py

<interfaces>
From src/run_forecast.py:
```python
def load_series_from_csv(path: Path, column: str, date_column: str | None): ...
```

From src/evaluate_forecast.py:
```python
def format_results(results: list[dict[str, float | int | str]]) -> pd.DataFrame: ...
```

From src/train.py:
```python
def preprocess_csv(file_path, batch_size=32, train_ratio=0.75):
    df = pd.read_csv(file_path, dtype='float64')
    df = df.dropna(axis=1, how='any')
    df = df.transpose()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write CSV-bridge and docs contract tests</name>
  <files>tests/test_materialize_dataset.py, tests/test_docs_contract.py</files>
  <action>Create test coverage for the Phase 1 bridge before implementation. The materialization tests must pin two output modes: a chronological single-series CSV that current forecasting/evaluation scripts can read directly, and a numeric-only training matrix export that matches the current preprocess_csv contract from src/train.py. The docs contract test must verify README.md and db/README.md include the compose bootstrap, ingest, discovery, integrity, and materialize commands from this phase. Keep both test files collection-safe before the new CLI exists.</action>
  <verify>
    <automated>python -m pytest --collect-only -q tests/test_materialize_dataset.py tests/test_docs_contract.py</automated>
  </verify>
  <done>The bridge and documentation tests collect cleanly and encode the exact downstream compatibility and documentation contract the implementation must satisfy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement PostgreSQL-to-CSV materialization and Phase 1 docs</name>
  <files>src/postgres_materialize_dataset.py, README.md, db/README.md</files>
  <behavior>
    - Test 1: a series_csv export writes timestamp-sorted close-price rows that src/run_forecast.py and src/evaluate_forecast.py can consume directly.
    - Test 2: a training_matrix export writes numeric-only series rows compatible with src/train.py preprocess_csv() without manual reshaping.
    - Test 3: repository docs explain the compose setup path, schema, ingestion, discovery, integrity verification, and materialization commands well enough to satisfy DISC-03 and D-12.
  </behavior>
  <action>Implement src/postgres_materialize_dataset.py as the required PostgreSQL-to-CSV bridge from research. Support filters consistent with the discovery dimensions, deterministic ascending timestamp order, and explicit output layouts for forecasting/evaluation versus fine-tuning consumers so D-12 is satisfied without refactoring existing model entrypoints to read PostgreSQL directly in Phase 1. Update README.md with the operator workflow and add db/README.md with the schema/table explanation, command sequence, and trust-boundary notes from this phase. Keep the scope to CSV materialization; do not introduce direct PostgreSQL readers into the TimesFM inference or training entrypoints yet.</action>
  <verify>
    <automated>python -m pytest -q tests/test_materialize_dataset.py tests/test_docs_contract.py -x</automated>
  </verify>
  <done>The repo has a working PostgreSQL-to-CSV bridge for current workflows, and the documentation explains the Phase 1 schema and command path without requiring reverse-engineering.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PostgreSQL dataset -> CSV files | Stored data is exported onto the local filesystem for downstream workflows |
| repository docs -> operator actions | Documentation drives database startup, ingestion, and export commands |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10 | T | materialization filters and sort order | mitigate | Reuse the discovery dimensions, validate layout choices explicitly, and keep timestamp ordering deterministic in exported CSVs. |
| T-01-11 | I | README.md and db/README.md | mitigate | Document commands with local-development placeholders only, avoid committing real secrets, and state the local trust boundary around the compose service. |
| T-01-12 | R | downstream dataset provenance | mitigate | Make the materialization CLI print or record the selected symbol/source/timeframe/date filters so exported files remain attributable to the PostgreSQL source slice. |
</threat_model>

<verification>
Run `python -m pytest -q tests/test_materialize_dataset.py tests/test_docs_contract.py`.
Smoke-test the command help with `python src/postgres_materialize_dataset.py --help`.
</verification>

<success_criteria>
Phase 1 ends with a documented PostgreSQL data layer, a working CSV bridge for existing workflows, and no requirement to hand-edit exports or reverse-engineer the schema before later modeling phases.
</success_criteria>

<output>
After completion, create `.planning/phases/01-set-up-postgresql-database-download-and-discover-all-data-ad/01-04-SUMMARY.md`
</output>

[pytest]
markers =
    unit: isolated deterministic tests that do not require external services
    contract: tests that validate documented or CLI-boundary contracts without service startup
    integration: tests that exercise multi-module or storage-backed behavior
    docker: tests that require Docker-managed services
